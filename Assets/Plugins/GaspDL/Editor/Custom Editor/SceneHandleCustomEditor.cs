using UnityEngine;
using UnityEditor;
/*
[CustomEditor(typeof(SceneHandleSO))]
public class SceneHandleCustomEditor : UnityEditor.Editor
{
    public override void OnInspectorGUI()
    {
        //base.OnGUI(position, property, label);
        base.OnInspectorGUI();

        SceneHandleSO sceneHandle = target as SceneHandleSO;

        GUILayout.Space(10);

        if (GUILayout.Button("Add Passage"))
        {
            sceneHandle.CreatePassageContainee();
        }
    }
}
*/